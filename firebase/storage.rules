rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read for uploaded lesson images; write requires auth and path under uploads/{uid}
    match /uploads/{uid}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == uid;
    }
  }
}

