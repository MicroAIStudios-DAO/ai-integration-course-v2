# Content drafts (review gate)

Drafts land here from the acquisition cycle. A draft is promoted to `public/blogs/<slug>.md` + registered in `src/content/blogPosts.ts` ONLY after it passes `node scripts/content-quality-gate.mjs`. Never publish straight from here.
